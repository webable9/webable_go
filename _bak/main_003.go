package main

import "fmt"

func main_03() {
    var a, b int = 10, 20
    fmt.Println(a, b)
    
    i, j, k := 1, 2, 3
    fmt.Println(i, j, k)
    
    var str1, str2 string = "Hello", "goorm"
    fmt.Println(str1, str2)
}